name: preferences
description: Dart package which manages the user preferences.
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  meta: ^1.1.8
  shared_preferences: ^0.5.12+4