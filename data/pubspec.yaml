name: data
description: A new Flutter package project.
version: 0.0.1
publish_to: 'none'
author:
  - Ahmed Shuaib <ahmed.shuaip600@gmail.com>

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  domain:
    path: ../domain
  # checking internet connection availability
  data_connection_checker: ^0.3.4
  # Json annotator
  json_annotation: ^3.1.1
  # local data persistence
  shared_preferences: ^0.5.12+4

  file_picker: ^2.1.6
  path: ^1.7.0
  # remote data
  firebase_core: "^0.7.0"
  firebase_auth: ^0.20.0+1
  cloud_firestore: "^0.16.0"
  googleapis: ^0.56.1
  google_sign_in: ^4.5.9

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^4.1.4
  json_serializable: ^3.5.1
  build_test: ^1.3.6
  build_runner: ^1.11.1

flutter: